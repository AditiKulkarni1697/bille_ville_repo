const admin_authentication = (req, res, next) => {
  //check in queries if the name and password is as desired then let it go ahead (temp)
  //create diff login route for admin. use query to check for name and password
};

module.exports = { admin_authentication };
